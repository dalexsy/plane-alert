# Agent entry point

Single source of truth: **[../speculation/AGENTS.md](../speculation/AGENTS.md)** (mindset, tickets, deploy, UI law).

Fleet rules and master spec live **only** in `speculation/`. This repo has pointers: `.cursor/rules/dryl-canonical.mdc` and `.cursor/rules/core-guardrails-first.mdc` — do not edit them.

Refresh pointers: `cd directory && npm run bootstrap:agent-rules`
